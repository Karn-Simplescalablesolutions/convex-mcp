{
  "name": "my-convex-db",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "convex": "latest",
    "supergateway": "latest"
  },
  "scripts": {
    "start": "supergateway --stdio \"sh -c 'CONVEX_DEPLOY_KEY=$CONVEX_DEPLOY_KEY npx convex mcp start'\" --port 8080 --key test"
  }
}